# Setup Guide

This guide will be expanded after the frontend and backend tooling are initialized.

Planned local setup:

1. Install frontend dependencies in `frontend/`.
2. Create a Python virtual environment in `backend/`.
3. Install backend dependencies from `backend/requirements.txt`.
4. Configure PostgreSQL and copy `backend/.env.example` to `backend/.env`.
5. Run frontend and backend development servers.
